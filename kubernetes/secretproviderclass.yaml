apiVersion: secret-store.csi.x-k8s.io/v1
kind: SecretProviderClass

metadata:
  name: aws-secrets

  spec:
  provider: aws

  parameters:
    objects: |
      - objectName: "rdsaccesss"
        objectType: "secretsmanager"